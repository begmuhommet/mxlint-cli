import { FC } from 'react';

interface IProps {
}

const PolicySummaryCard: FC<IProps> = () => {

    return <></>
    
    // <Card sx={{ width: '100%', height: '100%', ...flexColStart }}>
  //     <Typography variant="h2" color="text.secondary">
  //       {label}
  //     </Typography>
  //     <Box sx={{ ...commonStyles.flexCenter, gap: 1, color: 'inherit' }}>
  //       {iconName?.length > 0 && <AppIcon name={iconName} sx={{ stroke: statusColors[label.toLowerCase()] }} />}
  //       <Typography variant="h1" color="inherit" fontSize={40}>
  //         {value}
  //       </Typography>
  //     </Box>
  //   </Card>
};

export default PolicySummaryCard;